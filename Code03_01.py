katok = []
      
def add_data(friend):
    katok.append(None)
    kLen = len(katok)
    katok[kLen-1]=friend
add_data("A")
add_data("B")
add_data("C")
add_data("D")
add_data("E")

print(katok)
